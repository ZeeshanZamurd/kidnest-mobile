# Copy to android/keystore.properties and fill in (or run npm run android:release to auto-generate).
storeFile=kidonest-release.keystore
storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=kidonest
